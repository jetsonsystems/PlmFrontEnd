// Filename: photo-manager/models/image
define([
  'underscore',
  'backbone',
],
      function(_, Backbone) {
        var ImageModel = Backbone.Model.extend();

        return ImageModel;
      });
