// Filename: photo-manager/models/importer
define([
  'underscore',
  'backbone',
],
      function(_, Backbone) {
        var ImporterModel = Backbone.Model.extend();

        return ImporterModel;
      });
