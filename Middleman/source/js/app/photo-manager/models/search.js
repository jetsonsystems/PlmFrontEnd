// Filename: photo-manager/models/search
define([
    'underscore',
    'backbone',
],
    function(_, Backbone) {
        var SearchModel = Backbone.Model.extend();

        return SearchModel;
    });
